types.push(0);
positions.push([0,0]);
types.push(1);
positions.push([0,100]);
types.push(0);
positions.push([0,300]);

function save() {
    // Put the cookie saving data here.
    // I'll call save ocasionally. (Expert speller)
}